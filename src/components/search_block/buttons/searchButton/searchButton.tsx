import React, {Component} from 'react';
import "./style.css"

export default class SearchButton extends Component {
    render() {
        return (
            <button className="input_submit" type="submit">Request</button>
        );
    }

};
